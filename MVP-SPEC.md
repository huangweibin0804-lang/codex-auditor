# Codex-优化审查小助手 MVP 规格

> 版本：MVP 1.1
> 状态：富分析主流程已实现，进入重复性、人工一致性与外部试用验证
> 更新日期：2026-07-12
> 实现目录：`codex-auditor/`

## 1. 一句话定位

面向高频 Codex 用户的本地优先工作复盘 Skill：自动分析指定周期内的会话、项目、产物和协作过程，生成一份有数据、有证据、有判断、有下周期行动的本地 HTML 报告。

## 2. 核心价值

用户运行 Skill 后需要看清四件事：

1. 这段时间主要使用 Codex 做了什么。
2. 时间和精力主要投向了哪些项目与场景。
3. 哪些提问、任务拆解和协作方式造成了重复、返工或范围漂移。
4. 下一周期最值得改变的 1–3 个行为是什么。

核心产品类型为“工作质量与协作方式复盘”。Token、会话数量和磁盘占用只是辅助信息。

## 3. 目标用户

首版面向：

- 每周多次使用 Codex 的高频用户。
- 同时用 Codex 处理开发、研究、写作、视觉、知识管理或运营工作的个人用户。
- 已经开始建立 Skill、脚本、模板或 Agent Workflow，希望优化整体协作系统的用户。

首版不做团队管理、员工考核或计费审计。

## 4. 用户触发与交互

### 4.1 典型触发语句

```text
审查我最近的 Codex 使用情况。
复盘我最近 7 天的 Codex 工作。
生成一份最近 30 天的 Codex 月报。
分析 2026-07-01 到 2026-07-31 的 Codex 使用。
```

### 4.2 时间选择

- 用户已给出周期：直接执行。
- 用户未给出周期：只追问一次，提供 `1 天 / 7 天 / 30 天 / 自定义范围`。
- 自定义范围最长 90 天。
- 按用户本地时区展示日期；周趋势按周一至周日统计。

### 4.3 运行结果

Skill 完成后直接返回主交付物：

```markdown
[打开我的 Codex 月度复盘报告](/Users/<user>/.codex/reports/<report-id>.html)
```

同时给出 Markdown 归档版链接。JSON 与 manifest 是本地调试和证据文件，不作为普通用户的主交付物。

### 4.4 首次富分析告知

首次富分析前显示一句话：

> 富分析会将经筛选和脱敏的历史摘要发送到你当前使用的 Codex 模型链路；报告仅保存在本机。如需关闭，说“只做本地统计”。

该告知不新增确认步骤。本地记录告知版本，后续运行不重复展示，除非隐私边界或模型输入规格发生变化。

## 5. MVP 功能范围

### 5.1 P0：必须完成

1. 扫描活跃与归档 Codex JSONL 会话。
2. 计算活跃天数、会话数、事件覆盖分钟数和交互活动估算。
3. 生成日期趋势、小时活跃图和场景分布。
4. 基于会话语义进行跨会话项目归类。
5. 按项目估算投入，输出项目目标、工作内容、产物、进展、问题与下一步。
6. 深度审查投入最高的 3 个项目的提示词和对话推进过程。
7. 识别可验证异常、重复步骤、约束追加和会话内重定向模式。
8. 同时输出用户的进步、稳定优势和已形成的可复用工作流。
9. 生成 1–3 项可执行、可验证的下周期行动。
10. 输出零依赖单文件 HTML、Markdown、结构化 JSON 和证据 manifest。

### 5.2 P1：MVP 验证后考虑

- 与上一周期的报告自动对比。
- 追踪上周期行动是否改善。
- 用户手动合并、拆分或重命名项目。
- 定时周报、本地 Dashboard 和 Codex Plugin 形态。
- Windows 与 Linux 数据路径适配。

### 5.3 明确排除

- 云端账户与同步。
- 开发者后端、遥测、埋点或远程日志。
- 团队成员比较、绩效评分和监控。
- 提示词 0–100 分或“思维能力”评分。
- 从当前文件状态反推审计周期内的历史产物。
- 自动生成提示词模板、Skill 候选项或脚本候选项。

## 6. 隐私与威胁模型

### 6.1 对外承诺

> 本地优先、零开发者数据回传。Skill 作者无法读取用户会话、项目文件或复盘报告。

### 6.2 允许的数据流

```text
本机 Codex 日志
  → 本地脚本：扫描、适配、统计、证据定位、脱敏、渲染
  → 本地 HTML / Markdown / JSON / manifest

经筛选和脱敏的会话摘要
  → 用户当前使用的 Codex 模型请求链路
  → 富分析结果
  → 本地报告
```

### 6.3 禁止的数据流

- 不向 Skill 开发者服务器发送数据。
- 不接入第三方分析、埋点或遥测服务。
- 不生成公网报告链接。
- 不上传原始 JSONL、完整项目文件、完整工具输出或完整报告。
- HTML 不加载 CDN、远程字体、远程图片或统计脚本。

### 6.4 富分析默认值

- 富分析模式默认开启。
- 用户可明确要求“只做本地统计”或“不审查对话正文”来关闭语义审查。
- 进入模型的内容只能是主要项目的限量摘要与对话片段。
- 路径只保留文件名或项目名，禁止发送完整绝对路径。
- 明显密钥、Token、邮箱、电话和已知敏感路径在进入模型前替换。
- 高熵字符串、飞书/Lark 标识符等结构化敏感 ID 在进入模型前替换。
- 用户正文先剥离 Codex App 自动附加的环境上下文，防止系统路径、权限与插件信息进入语义输入。
- 模型链路的数据处理和保留受用户当前 Codex 服务与账户设置约束；Skill 不额外收集该数据。

## 7. 数据源与扫描边界

### 7.1 会话数据

```text
~/.codex/sessions
~/.codex/archived_sessions
~/.codex/session_index.jsonl
```

扫描器只读原始数据，不改写会话日志。

### 7.2 文件工作区

规格固定为：

> 项目归类以会话语义为主，`workspace_roots` 只负责限定文件扫描范围，`cwd` 仅作为缺失时的回退。

扫描顺序：

1. 优先从 `turn_context.payload.workspace_roots` 提取已授权工作区。
2. `workspace_roots` 缺失时使用 `session_meta.payload.cwd`。
3. 两者均缺失时，只分析会话，不扫描文件系统。
4. 禁止默认遍历整个 Home、Documents 或用户磁盘。

为控制扫描时间和敏感面，当前文件扫描只针对投入最高的 3 个语义项目，并遵守：

- 每个工作区最多遍历 3 层目录、最多 2,000 个目录项。
- 跳过 `.git`、`node_modules`、构建目录、缓存目录和隐藏目录。
- 只读取路径、类型、大小和修改时间等元数据；默认不读取文件正文。
- 单文件大于 5 MB 时只保留路径元数据。
- 达到上限立即停止该工作区扫描，并在覆盖率中说明。

### 7.3 产物分类

**已确认产物：**

- `patch_apply_end` 中可定位的文件变更。
- 图片生成事件中的本地保存路径。
- 已知工具结构化字段明确返回的写入路径。

**当前相关文件：**

- 仅在 `workspace_roots` 限定范围内扫描。
- 用于帮助用户找回当前项目文件。
- 必须标注“当前存在”，不宣称由本周期 Codex 生成。

MVP 不通过 mtime、当前 Git 状态或当前文件内容反推历史产物。Git 仅允许用 `git rev-parse --show-toplevel` 辅助去重工作区根目录。

## 8. 总体架构

```text
Codex Skill
  ├─ 识别用户意图与时间范围
  ├─ 调用本地确定性审计脚本
  ├─ 组织富分析模型请求
  └─ 交付本地报告链接

本地脚本
  ├─ JSONL 适配
  ├─ 时间与事件统计
  ├─ 工作区限定与产物证据提取
  ├─ 脱敏与语义输入生成
  ├─ 证据 manifest
  └─ HTML / Markdown / JSON 渲染

Codex 模型
  ├─ 跨会话项目聚类
  ├─ 主要项目摘要
  ├─ 提示词与对话推进审查
  ├─ 进步与工作模式总结
  └─ 下周期行动建议
```

### 8.1 责任边界

- 脚本负责可复现的事实、计算、限额、证据、脱敏与渲染。
- 模型负责语义聚类、文本理解、根因推断、进步总结与建议。
- 模型不重新计算时间、事件数量、失败率或产物数。
- 所有模型判断都需保留所引用的 `evidence_id`。

## 9. 确定性数据层

### 9.1 日志适配

首版适配当前 macOS Codex Desktop / CLI JSONL，使用版本化适配器 `codex_jsonl_adapter_v1`。

| 标准事件 | JSONL 映射 | 用途 |
|---|---|---|
| `USER_MESSAGE` | `event_msg` + `payload.type=user_message` | 时间锚点、提示词审查 |
| `AGENT_MESSAGE` | `event_msg` + `payload.type=agent_message` | 时间锚点、对话推进 |
| `ASSISTANT_MESSAGE_FALLBACK` | `response_item` + `payload.type=message` + `role=assistant` | 缺失 Agent 事件时回退 |
| `TASK_COMPLETE` | `event_msg` + `payload.type=task_complete` | 软消息链边界辅助 |
| `PATCH_RESULT` | `event_msg` + `payload.type=patch_apply_end` | 已确认产物、明确失败 |
| `IMAGE_RESULT` | `event_msg` + `payload.type=image_generation_end` | 已确认图片产物 |
| `FUNCTION_RESULT` | `response_item` + `payload.type=function_call_output` | 执行证据 |
| `CUSTOM_TOOL_RESULT` | `response_item` + `payload.type=custom_tool_call_output` | 执行证据 |

未知事件、时间戳缺失、角色缺失与备用消息无法配对时，忽略该事件并计入报告的“数据缺口”。

### 9.2 时间口径

预设时间范围按运行机器的本地自然日计算：`1 天`为当天零点至当前时刻，`7 天`为含当天在内的最近 7 个自然日，`30 天`为含当天在内的最近 30 个自然日。自定义范围使用用户明确给出的起止时间。

报告必须同时展示：

| 指标 | 定义 |
|---|---|
| 事件覆盖分钟数 | 包含至少一个用户消息或可见 Agent 回复的去重自然分钟数 |
| Codex 交互活动估算 | 根据消息锚点之间的有限时间区间估算的协作活动 |

区间规则：

- 用户消息 → Agent 回复：最多计 2 分钟。
- Agent 回复 → 下一条用户消息：最多计 15 分钟。
- Agent → Agent：不延长时间。
- 工具开始、工具完成、Token、流式事件和后台事件不延长时间。
- 并行会话取时间区间并集，重叠分钟数单独展示。

报告用语固定为“交互活动估算”，不称为真实工时、工作时长或总投入工时。

### 9.3 用户消息链

使用 `user_message_chain_v2`：

- 会话首条用户消息开启新链。
- 距上一条用户消息超过 30 分钟时硬切链。
- 以“新任务、另一个任务、换个任务、换个话题、现在开始新”开头时硬切链。
- 以“接下来做”开头，且前 5 分钟存在有效 `TASK_COMPLETE` 时软切链。
- `TASK_COMPLETE` 单独出现不切链。

产品需明确说明：未显式标记的新任务可能保留在同一消息链中；相关诊断只表示会话内后续补充或重定向模式。

### 9.4 场景分类

统计标签使用版本化的本地规则分类器，模型可解释标签，不改写统计结果。

固定类别：

1. 开发与调试。
2. 研究与检索。
3. 写作与内容生产。
4. 文件与知识管理。
5. 设计与可视化。
6. 运营协作。
7. 环境配置与排障。
8. 其他/待确认。

## 10. 富分析与项目归类

### 10.1 双层分类结构

```text
本地工作区：负责证据、文件扫描和安全边界
        ↓
语义项目主题：负责用户可读的跨会话项目聚类
```

项目主题可以横跨多个会话、Skill 和工作区，例如“Agent 学习与内容体系”。报告必须将项目主题标注为“模型归纳”，同时保留被归入的会话和证据 ID。

### 10.2 会话项目聚类

所有会话都进入确定性统计。富分析项目聚类最多处理 120 个会话，每条会话先生成 `ThreadDigest v1`：

```text
thread_ref
会话标题，最多 120 字
首条用户请求，脱敏后最多 500 字
末条用户请求，脱敏后最多 500 字
时间范围
交互活动估算
前 5 个工具类型及次数
已确认产物文件名，最多 10 个
本地场景标签
规则发现 ID
```

聚类请求每批最多 40 个 `ThreadDigest`，总字符数最多 32,000，最多 3 批。使用多批时只增加 1 次合并请求。

会话超过 120 个时，按“交互活动估算、已确认产物数、规则发现数、最近时间、`thread_ref`”固定排序选取前 120 个。其余会话标记为 `semantic_unclassified`，仍进入总时间、场景和事件统计。报告必须展示语义归类覆盖率。

每条会话只能有一个 `primary_project_id`。可选关联项目只用于说明语义关系，不分配时间、事件和产物，从而避免项目占比重复计算。

### 10.3 主要项目深度审查

按交互活动估算选择投入最高的 3 个语义项目。

每个项目最多选择 5 个会话：

- 投入时间最高的 2 个。
- 确认产物最多的 1 个。
- 规则异常最多的 1 个。
- 最近发生的 1 个。
- 重复的会话去重，不足 5 个时不补齐。

对话轮次定义为一条 `USER_MESSAGE` 及下一条用户消息前的首条可见 `AGENT_MESSAGE`。每个项目按固定槽位最多选择 30 组：

| 证据类型 | 最多轮次 |
|---|---:|
| 首轮目标与最早验收框定 | 3 |
| 约束追加 | 5 |
| 明确异常与规则发现 | 6 |
| 已确认产物节点 | 5 |
| 重定向与重要转折 | 5 |
| 末轮结果 | 3 |

同一用户消息证据 ID 去重。各槽位内按“明确严重度降序、时间升序、证据 ID 升序”选取。保留槽位最多产生 27 组，剩余 3 组从未选时间线中等距抽取。槽位证据不足时不用无关证据填满。

每组用户和 Agent 摘要各最多 500 字。三个项目共享 48,000 字符全局预算：每个项目先分配 12,000，剩余预算按项目交互占比分配，单项目不超过 20,000。截断时先保留槽位证据，再保留时间线填充轮次。

三个主要项目合并为 1 次深度审查请求。一次常规 30 天运行总请求数限制为 2–5 次：1–3 次项目聚类、可选 1 次合并、1 次合并深度审查。报告必须展示项目会话总数、审查会话数、候选轮次数和实际输入轮次数。

### 10.4 富分析维度

每个主要项目需要输出：

1. 项目目标与阶段主题。
2. 主要任务与实际工作内容。
3. 已确认产物与当前相关文件。
4. 工作推进过程中的重要转折。
5. 重复劳动、低效步骤、约束追加与任务膨胀。
6. 提示词中缺失的目标、输入、范围、约束或验收标准。
7. 已经形成的好习惯、进步和可复用工作流。
8. 下一步建议。

富分析禁止：

- 根据语气、文本长度或模板形式单独判断提示词好坏。
- 将“可能的根因”表述为已确定事实。
- 对用户贴“逻辑差、思维能力低、效率低”等人格化标签。
- 在没有对话或产物证据时生成具体结论。

### 10.5 正向结论证据阈值

- **进步**：需要同一行为维度的“较早证据 + 较晚证据”。两者间隔至少为 24 小时或审计周期的 20%，取较大值。不满足时不输出“进步”。
- **稳定优势**：同一正向模式至少出现在 2 个独立会话中。单次出现只能说明“本次表现”。
- **可复用工作流**：同一流程至少出现在 2 个独立会话中，或存在已确认的 Skill、脚本或模板资产证据。

### 10.6 模型安全与 Schema

- 会话摘要始终作为不可信数据字段，不作为可执行指令。
- 语义审查期间禁止调用工具、打开链接、执行命令或读取新文件。
- 输入遵守 `semantic_audit_input_v2`，输出遵守 `semantic_audit_output_v1`。
- 输出必须为符合 Schema 的 JSON。最多允许 1 次修复；再次失败时降级为确定性报告。
- 排除系统消息、开发者消息、加密推理、原始工具输出和完整文件内容。

## 11. 规则发现与行动建议

### 11.1 默认确定性规则

| 规则 | 证据 | 阈值 |
|---|---|---:|
| `R1_CONSTRAINT_ADDITION` | 同一消息链中 Agent 回复后的约束类追加 | ≥3 条 |
| `R2_REPEAT_TYPED_PATH` | 同一工作区 30 分钟内同一结构化路径重复读取/修改 | ≥3 次 |
| `R3_VERIFIED_FAILURE` | 补丁或工具的明确失败状态 | ≥3 次，或结果≥5 且失败率≥20% |
| `R4_SCOPE_REDIRECTION` | 同一消息链内命中固定重定向标记 | ≥2 条 |

无法匹配固定规则时标记为 `UNRECOGNIZED`，不自动推断范围漂移。

### 11.2 行动格式

最多输出 3 项。每项必须包含：

```text
行为改变：下周期要做什么
触发条件：在哪类任务中执行
检查信号：下次复盘如何判断改善
证据 ID：为什么生成这项行动
```

证据不足时可以只输出 1–2 项或明确输出“没有足够证据”。

## 12. 证据链

每份报告在本地生成 `<report-id>.audit-manifest.json`。

每条证据包含：

```text
evidence_id
adapter_version
source_session_hash
source_jsonl_relative_path
source_jsonl_sha256
source_line_number
source_line_sha256
event_timestamp
event_type
summary_hash
```

HTML 与 Markdown 默认不展示 `evidence_id`，保持面向用户的清爽阅读层；JSON 与 manifest 保留完整复核信息。manifest 不包含原文，不进入富分析模型输入，不上传。验证器重新计算源文件与源行哈希，用于判断底层记录后续是否发生变化。

报告还需保留分析来源信息：

```text
model_id
analysis_prompt_version
semantic_input_schema_version
semantic_output_schema_version
adapter_version
message_chain_version
scenario_classifier_version
action_rules_version
```

模型升级或规则变更后，对比报告必须同时展示版本差异，不将不同分析来源的结果当作纯时间趋势。

## 13. HTML 报告交付

所有页面固定显示“本地私密报告”。报告目录权限为 `0700`，HTML、Markdown、JSON 和 manifest 文件权限为 `0600`。

### 13.1 页面结构

1. 顶部导航：报告名、时间范围、页内导航、Markdown 和 PDF 下载入口。
2. Hero：“Codex 月度工作复盘”与一句核心判断。
3. 总览指标：活跃时间估算、会话数、活跃天数、项目数、已确认产物。
4. 时间与精力：日期趋势（每项带 `mins`）、小时活跃图、按工作区数量降序排列的场景分布；场景分布卡片跨两列铺满内容区域，并行重叠分钟与活跃日合并在卡片内。
5. 项目版图：项目投入占比与主要项目卡片。
6. 项目复盘：做了什么、卡点是什么、优化方案；三段内容纵向排列，稳定优势统一放入工作模式。
7. 工作模式：主要问题、本周期进步和稳定优势。
8. 下周期行动：1–3 项可验证行动。
9. 数据口径：不单独占用内容卡片，折叠展示数据缺口、估算方法、富分析覆盖率和隐私说明。

页面展示层隐藏 evidence ID 和对应小字，底层 JSON、语义包与 manifest 继续保留复核链路。
项目复盘页面不展示内部 `fact / inference / recommendation` 标签；这些类型只用于内部校验和结构化输出。项目复盘采用三段纵向阅读路径，减少分栏跳读。

### 13.2 视觉规范

视觉基于 Refero Styles 中的 Sana Agents 设计语言：

```text
Paper White  #ffffff
Ink Black    #0a1217
Frost Wash   #e4eff7
Stone Gray   #85898b
Obsidian     #000000
Electric Lime #cdfe00
```

固定规则：

- 卡片与面板圆角 `24px`。
- 按钮与标签圆角 `9999px`。
- 按钮高度 `44px`。
- 间距系统 `8 / 16 / 24 / 32 / 64px`。
- 页面最大宽度 `1200px`。
- 浅色区域主 CTA 使用 Ink Black。
- Electric Lime 只用在深色面板的低频关键强调。
- 禁止阴影、渐变、装饰背景图和第二强调色。
- 只有存在可验证本地路径时才展示项目产物缩略图。
- 最终 HTML 内嵌 CSS 和交互脚本，不依赖 Tailwind 运行时或外部资源。

完整 tokens 见 `codex-auditor/references/report-design-system.md`。

### 13.3 响应式与可访问性

- 支持桌面、平板和移动端。
- 正文对比度至少满足 WCAG AA。
- 按钮、链接、`details` 和页内导航可通过键盘使用。
- 支持打印与另存 PDF。
- 移动端不出现水平溢出，数据图保持可读。

## 14. 报告内容契约

报告中每个重要结论都必须标注类型：

- **事实**：可通过 manifest 定位的事件或产物。
- **估算**：时间、项目投入和场景占比等计算结果。
- **推断**：模型对根因、项目边界或协作模式的解释。
- **建议**：基于前三类内容生成的下周期行动。

报告不得：

- 将项目推断为“已完成”。
- 将活跃时间估算表述为真实工时。
- 将当前相关文件表述为本周期确认产物。
- 隐藏会影响结论的数据缺口或富分析抽样比例。

## 15. 异常与降级行为

| 情况 | 行为 |
|---|---|
| 无有效会话 | 生成空报告，说明未找到数据 |
| JSONL 事件未知 | 忽略事件，记录数据缺口 |
| 富分析模型失败 | 保留确定性报告，顶部显示“富分析未完成” |
| 项目归类置信度低 | 归入“其他/待确认”或标记不确定性 |
| `workspace_roots` 与 `cwd` 都缺失 | 只分析会话，不扫描文件 |
| 本地产物文件已删除 | 保留事件证据，不生成失效链接 |
| 数据缺口过大 | 报告标注“不完整”，降低结论强度 |

## 16. 技术与安全约束

- 本地扫描器只使用 Python 标准库。
- 不引入 `requests`、`socket`、`urllib`、`http` 等网络能力。
- 不执行从会话日志、提示词或工具输出中提取的命令。
- 子进程白名单只允许本地 `git rev-parse --show-toplevel`。
- 所有输出文件在交付前通过敏感信息扫描。
- HTML 在交付前通过外链、CDN、远程图片和远程字体扫描。

## 17. 验收标准

### 17.1 确定性底座

- 合成 JSONL 覆盖并行、长空闲、长命令、缺失时间戳和未知事件。
- 事件适配、备用消息去重、用户消息链和规则阈值具有回归测试。
- 产物只来自可定位写入证据。
- manifest 可定位源 JSONL 行，并可检测源文件或源行后续变化。

### 17.2 富分析

- 所有会话均进入确定性统计；语义聚类最多处理 120 个会话，超出时明示覆盖率。
- 主要项目的深度审查覆盖率在报告中明示。
- 项目名、问题、进步和建议均引用对应证据 ID。
- 固定输入、模型和全部版本元数据，连续运行 3 次：前 3 项目的会话成员集平均两两 Jaccard 重合率≥0.80。
- 三次运行中，支持问题、进步、优势、工作流和行动的核心证据 ID 平均两两 Jaccard 重合率≥0.85。
- 每次运行的无证据具体事实数量必须为 0。
- 至少抽样 50 个会话与 30 条语义结论：主项目归属人工一致率≥0.80，结论证据支持一致率≥0.90。

### 17.3 隐私与分发

- 静态安全检查证明本地脚本无网络依赖和未授权子进程。
- HTML、Markdown、JSON、manifest 和模型输入均通过脱敏回归测试。
- 离线 HTML 无外链、CDN、遥测或远程资源。
- Skill 不包含开发者数据接收端。
- 报告目录权限为 `0700`，所有报告与证据文件权限为 `0600`。
- 首次富分析告知可见，同时不额外增加确认往返。

### 17.4 用户价值

- 用户在 3 分钟内能说清本周期的主要投入、一个协作问题和一项下周期行动。
- 至少 50% 的试用用户认为报告发现了之前未明确意识到的问题。
- 至少 33% 的试用用户愿意在下一周期再次运行。

## 18. 当前实现状态

### 18.1 已完成

- Skill 骨架与触发描述。
- JSONL 本地扫描和基础适配器。
- 事件覆盖与交互活动估算。
- 工作区别名、用户消息链、确定性规则和证据 manifest。
- 本地 HTML / Markdown / JSON 生成。
- Refero Sana Agents 风格报告 Demo。
- 标准库依赖和网络能力静态检查。
- 本地私密报告标识、`0700` 目录权限和 `0600` 文件权限。
- 分析提示、语义输入/输出、场景分类器和行动规则版本字段。
- `semantic_audit_input_v2` 与 `semantic_audit_output_v1` 规格。
- 从 `turn_context.workspace_roots` 提取受限文件扫描根。
- `ThreadDigest v1`、确定性抽样、最多 3 批项目聚类与项目唯一归属。
- 主项目深度审查的固定轮次槽位、总字符预算和最多 3 个行动项约束。
- 项目聚类与深度审查输出的 Schema、证据引用和正向结论阈值校验。
- 语义项目、主要问题、本周期进步、稳定优势、可复用工作流和行动项写回 HTML / Markdown / JSON。
- 报告 HTML 提供同目录 PDF 下载入口；PDF 使用本机 Chromium 渲染，保留报告视觉布局并以 `0600` 权限保存。
- 报告展示层已完成视觉收口：活跃趋势显示 `mins`，场景按工作区数量降序，统计信息并入场景卡片，HTML/PDF/Markdown 隐藏 evidence ID 与复核小字。
- 主要项目复盘已改为“做了什么 / 卡点是什么 / 优化方案”三段纵向结构，稳定优势统一留在工作模式，隐藏内部类型标签。
- 高熵字符串、飞书/Lark 标识符、绝对路径脱敏和 Codex App 环境上下文剥离。
- 本地自然日预设、已知忽略事件与未知事件分离、结构解析覆盖率展示。
- Manifest 源文件缓存、模型输出权限加固和富分析失败降级。

### 18.2 待验证与增强

- 同一真实数据连续 3 次运行的项目成员与核心证据重合率验证。
- 人工标注主项目归属与结论证据支持一致性。
- 30 天、自定义时间范围和真实多批聚类案例。
- 桌面、平板、移动端完整视觉与键盘操作验收。
- 模型输出校验失败时的一次自动修复提示优化。
- 至少 5 名外部用户的安装与首份报告验证。

### 18.3 2026-07-12 本机 7 天案例

- 范围：2026-07-06 至 2026-07-12，共 7 个本地自然日。
- 有效会话 37 个，进入语义聚类 37 个，语义覆盖率 100%，形成 7 个项目。
- 事件覆盖 341 分钟，交互活动估算 620 分钟，并行重叠 81 分钟。
- 扫描记录 42,564 条；适配 18,495 条；已知忽略 24,069 条；未知事件 0 条；结构解析覆盖率 100%。该覆盖率只证明本机当前日志版本，不外推到其他 Codex 版本。
- 深度审查覆盖 3 个主要项目、8 个会话、253 个候选轮次和 49 个输入轮次。
- 最终生成 3 个项目复盘、2 个主要问题、1 项进步、2 项稳定优势、1 个可复用工作流和 3 个行动项；全部判断通过证据引用校验。
- 真实运行发现并修复：高熵凭证进入模型包、App 环境上下文混入正文、深度抽样挤掉最新结果轮次、UTC 与本地日期错位、7 天预设跨 8 个日期、7 天报告误用月度标题、已知忽略事件被误报为数据缺口、模型输出权限为 `0644`、Manifest 验证重复读取大文件导致性能异常。
- 当前案例的 Manifest 在报告生成后会因活跃 JSONL 继续追加而显示 `source_changed`。验证器在静态样本上已通过“未修改成功、修改后失败”的双向测试。

## 19. 下一阶段验证顺序

1. 对同一份冻结输入连续运行 3 次，计算项目成员和核心证据重合率。
2. 冻结一份人工标注集，验证主项目归属和结论证据支持一致性。
3. 运行本人 30 天与自定义范围案例，覆盖多批聚类、合并与预算上限。
4. 完成桌面、平板、移动端视觉与键盘操作验收。
5. 根据失败样本优化一次自动修复提示和降级说明。
6. 满足发布闸门后进入至少 5 名外部用户的 GitHub 试用。

## 20. GitHub 试用发布闸门

以下条件全部通过后才可面向外部用户：

1. 7 天、30 天和自定义范围均可稳定生成报告。
2. 当前支持的 Codex JSONL 样本解析率达到 90% 以上；未知事件有数据缺口说明。
3. 富分析在多类真实工作样本上不出现无证据事实。
4. 三次重复运行达到项目成员重合率≥0.80、核心证据重合率≥0.85，无证据具体事实数量为 0。
5. 人工审查达到主项目归属一致率≥0.80和结论证据支持一致率≥0.90。
6. 完成静态安全、输出脱敏、模型输入脱敏与 HTML 外链扫描。
7. Skill 作者无法从产品架构中收集用户记录。
8. HTML 在桌面与移动端通过完整视觉验收。
9. 至少 5 名外部试用者成功安装并生成首份报告。

## 21. 产品成功标准

MVP 的首要成功信号：

> 用户从报告中发现至少一个之前未明确意识到的工作或协作问题，并愿意在下一周期再次运行。

报告生成速度、HTML 视觉效果和数据完整度是必要质量条件，不单独代表产品价值成立。
